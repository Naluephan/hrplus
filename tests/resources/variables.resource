*** Settings ***
Variables  env.py

*** Variables ***
# Default fallback values can be kept here if needed, butenv.py takes precedence if loaded as a Variable file.
# However, to be purely env-driven, we just rely on env.py
